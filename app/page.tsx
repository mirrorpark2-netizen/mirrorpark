'use client';

import { useEffect, useMemo, useState } from 'react';
import {
  BarChart3, Bell, Check, ChevronDown, Clipboard, Clock3, FileText,
  Gauge, GitBranch, History, Inbox, LayoutDashboard, Link2, LogIn, LogOut, Menu, Minus,
  Plus, ReceiptText, Settings2, ShieldCheck, UserPlus, UserRound, Users, Wrench, X,
} from 'lucide-react';

type View = 'Dashboard' | 'Time Clock' | 'New Invoice' | 'Invoice History' | 'Employees' | 'Join Team' | 'Reports' | 'Admin Controls';
type Invoice = { id: string; customer: string; date: string; total: number; lines: string[]; message: string };
type Service = { id: string; name: string; price: number; unit: string; icon: string };
type Employee = { name: string; role: string; week: string; month: string; status: string; initials: string; invoices: number; discord?: string; gameId?: string; mobile?: string; cid?: string };
type MechanicType = { id: string; name: string; level: number };
type Applicant = { id: string; discord: string; gameId: string; gameName: string; mobile: string; cid: string; status: 'pending'; requestedAt: string; assignedRole: string };

const defaultServices: Service[] = [
  { id: 'door', name: 'Door', price: 100, unit: '1× Scrap', icon: 'DR' },
  { id: 'window', name: 'Window', price: 60, unit: '1× Glass', icon: 'WN' },
  { id: 'tyre', name: 'Tyre', price: 50, unit: '1× Rubber', icon: 'TY' },
  { id: 'repair-kit', name: 'Advance Repair Kit', price: 700, unit: 'Repair components', icon: 'RK' },
  { id: 'motor-oil', name: 'Motor Oil', price: 700, unit: 'Premium synthetic', icon: 'OL' },
  { id: 'brake-pad', name: 'Brake Pad', price: 400, unit: 'Front or rear set', icon: 'BP' },
  { id: 'tow', name: 'Tow Service', price: 250, unit: 'Citywide recovery', icon: 'TW' },
];

const defaultEmployees: Employee[] = [
  { name: 'Vex Walker', role: 'Boss', week: '34h 20m', month: '138h 10m', status: 'On duty', initials: 'VW', invoices: 42 },
  { name: 'Mila Stone', role: 'Mechanic', week: '29h 05m', month: '121h 44m', status: 'On duty', initials: 'MS', invoices: 31 },
  { name: 'Rico Hale', role: 'Tow Operator', week: '21h 44m', month: '96h 08m', status: 'Off duty', initials: 'RH', invoices: 18 },
  { name: 'Avery Knox', role: 'Apprentice', week: '17h 30m', month: '72h 55m', status: 'Off duty', initials: 'AK', invoices: 12 },
];

const defaultMechanicTypes: MechanicType[] = [
  { id: 'boss', name: 'Boss', level: 1 },
  { id: 'manager', name: 'Manager', level: 2 },
  { id: 'lead', name: 'Lead Mechanic', level: 3 },
  { id: 'senior', name: 'Senior Mechanic', level: 4 },
  { id: 'mechanic', name: 'Mechanic', level: 5 },
  { id: 'tow', name: 'Tow Operator', level: 6 },
  { id: 'apprentice', name: 'Apprentice', level: 7 },
];

const seedInvoices: Invoice[] = [
  { id: 'MP-1846', customer: 'J. Hernandez', date: '02 Sep 2026, 20:42', total: 860, lines: ['2× Door — $200', '1× Window — $60', 'Custom labor: Body alignment — $600'], message: 'Thank you for choosing Mirror Park Mechanics.' },
  { id: 'MP-1845', customer: 'N. Carter', date: '01 Sep 2026, 23:15', total: 950, lines: ['1× Advance Repair Kit — $700', '1× Tow Service — $250'], message: 'Thank you for choosing Mirror Park Mechanics.' },
  { id: 'MP-1844', customer: 'S. Price', date: '01 Sep 2026, 19:08', total: 800, lines: ['1× Motor Oil — $700', '1× Door — $100'], message: 'Thank you for choosing Mirror Park Mechanics.' },
];

const money = (value: number) => new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', maximumFractionDigits: 0 }).format(value);

function Panel({ children, className = '' }: { children: React.ReactNode; className?: string }) {
  return <section className={`rounded-2xl border border-[#1e3d48] bg-[#0b1820] shadow-[0_18px_50px_rgba(0,0,0,.16)] ${className}`}>{children}</section>;
}

export default function Home() {
  const [view, setView] = useState<View>('Dashboard');
  const [checkedIn, setCheckedIn] = useState(false);
  const [seconds, setSeconds] = useState(0);
  const [items, setItems] = useState<Record<string, number>>({});
  const [customer, setCustomer] = useState('');
  const [discount, setDiscount] = useState(0);
  const [laborName, setLaborName] = useState('');
  const [laborPrice, setLaborPrice] = useState(0);
  const [catalog, setCatalog] = useState<Service[]>(defaultServices);
  const [staff, setStaff] = useState<Employee[]>(defaultEmployees);
  const [mechanicTypes, setMechanicTypes] = useState<MechanicType[]>(defaultMechanicTypes);
  const [applications, setApplications] = useState<Applicant[]>([]);
  const [applicationForm, setApplicationForm] = useState({ discord: '', gameId: '', gameName: '', mobile: '', cid: '' });
  const [applicationStatus, setApplicationStatus] = useState('');
  const [businessName, setBusinessName] = useState('Mirror Park Mechanics');
  const [adminMessage, setAdminMessage] = useState('Thank you for choosing Mirror Park Mechanics. Drive safe and visit us again.');
  const [invoices, setInvoices] = useState<Invoice[]>(seedInvoices);
  const [copyStatus, setCopyStatus] = useState('');
  const [menuOpen, setMenuOpen] = useState(false);
  const [employeePreview, setEmployeePreview] = useState(false);
  const isAdmin = !employeePreview;

  useEffect(() => {
    const savedMessage = window.localStorage.getItem('mp-admin-message');
    const savedInvoices = window.localStorage.getItem('mp-invoices');
    const savedCatalog = window.localStorage.getItem('mp-service-catalog');
    const savedStaff = window.localStorage.getItem('mp-employees');
    const savedTypes = window.localStorage.getItem('mp-mechanic-types');
    const savedApplications = window.localStorage.getItem('mp-applications');
    const savedBusinessName = window.localStorage.getItem('mp-business-name');
    if (savedMessage) setAdminMessage(savedMessage);
    if (savedBusinessName) setBusinessName(savedBusinessName);
    if (savedCatalog) { try { setCatalog(JSON.parse(savedCatalog)); } catch { /* retain default catalog */ } }
    if (savedStaff) { try { setStaff(JSON.parse(savedStaff)); } catch { /* retain default employees */ } }
    if (savedTypes) { try { setMechanicTypes(JSON.parse(savedTypes)); } catch { /* retain default hierarchy */ } }
    if (savedApplications) { try { setApplications(JSON.parse(savedApplications)); } catch { /* retain empty queue */ } }
    if (savedInvoices) { try { setInvoices(JSON.parse(savedInvoices)); } catch { /* retain demo history */ } }
  }, []);

  useEffect(() => {
    if (!checkedIn) return;
    const timer = window.setInterval(() => setSeconds(value => value + 1), 1000);
    return () => window.clearInterval(timer);
  }, [checkedIn]);

  const subtotal = useMemo(() => catalog.reduce((sum, service) => sum + service.price * (items[service.id] || 0), laborPrice), [catalog, items, laborPrice]);
  const total = Math.max(0, subtotal * (1 - discount / 100));
  const currentUser = staff[0] || defaultEmployees[0];
  const duration = `${String(Math.floor(seconds / 3600)).padStart(2, '0')}:${String(Math.floor((seconds % 3600) / 60)).padStart(2, '0')}:${String(seconds % 60).padStart(2, '0')}`;
  const changeQty = (id: string, change: number) => setItems(previous => ({ ...previous, [id]: Math.max(0, (previous[id] || 0) + change) }));

  const makeInvoiceText = (invoice: Invoice) => [
    businessName.toUpperCase(),
    `INVOICE ${invoice.id}`,
    `Customer: ${invoice.customer}`,
    `Mechanic: ${currentUser.name}`,
    `Date: ${invoice.date}`,
    '--------------------------------',
    ...invoice.lines,
    '--------------------------------',
    `TOTAL: ${money(invoice.total)}`,
    '',
    invoice.message,
  ].join('\n');

  const copyText = async (text: string) => {
    try {
      await navigator.clipboard.writeText(text);
    } catch {
      const area = document.createElement('textarea');
      area.value = text; area.style.position = 'fixed'; area.style.opacity = '0';
      document.body.appendChild(area); area.select(); document.execCommand('copy'); area.remove();
    }
  };

  const submitInvoice = async () => {
    if (subtotal <= 0) { setCopyStatus('Add at least one item or labor charge.'); return; }
    const id = `MP-${1847 + invoices.length}`;
    const lines = catalog.filter(service => items[service.id]).map(service => `${items[service.id]}× ${service.name} — ${money(service.price * items[service.id])}`);
    if (laborPrice > 0) lines.push(`Custom labor: ${laborName.trim() || 'Labor charge'} — ${money(laborPrice)}`);
    if (discount > 0) lines.push(`Discount: ${discount}%`);
    const invoice: Invoice = { id, customer: customer.trim() || 'Walk-in customer', date: new Date().toLocaleString('en-GB', { dateStyle: 'medium', timeStyle: 'short' }), total, lines, message: adminMessage };
    const nextInvoices = [invoice, ...invoices];
    setInvoices(nextInvoices);
    window.localStorage.setItem('mp-invoices', JSON.stringify(nextInvoices));
    await copyText(makeInvoiceText(invoice));
    setCopyStatus(`${id} saved and copied to clipboard.`);
  };

  const resetInvoice = () => { setItems({}); setCustomer(''); setDiscount(0); setLaborName(''); setLaborPrice(0); setCopyStatus(''); };
  const saveAdminSettings = () => {
    window.localStorage.setItem('mp-admin-message', adminMessage);
    window.localStorage.setItem('mp-service-catalog', JSON.stringify(catalog));
    window.localStorage.setItem('mp-employees', JSON.stringify(staff));
    window.localStorage.setItem('mp-mechanic-types', JSON.stringify(mechanicTypes));
    window.localStorage.setItem('mp-applications', JSON.stringify(applications));
    window.localStorage.setItem('mp-business-name', businessName);
    setCopyStatus('All admin changes saved.');
  };

  const submitApplication = () => {
    if (Object.values(applicationForm).some(value => !value.trim())) { setApplicationStatus('Complete all five required fields.'); return; }
    const application: Applicant = { id: `APP-${Date.now()}`, ...applicationForm, status: 'pending', requestedAt: new Date().toLocaleString('en-GB', { dateStyle: 'medium', timeStyle: 'short' }), assignedRole: mechanicTypes.find(type => type.name !== 'Boss')?.name || 'Mechanic' };
    const next = [application, ...applications];
    setApplications(next); window.localStorage.setItem('mp-applications', JSON.stringify(next));
    setApplicationForm({ discord: '', gameId: '', gameName: '', mobile: '', cid: '' });
    setApplicationStatus('Application submitted. An admin must approve your account.');
  };

  const acceptApplication = (application: Applicant) => {
    const employee: Employee = { name: application.gameName, role: application.assignedRole, week: '0h 00m', month: '0h 00m', status: 'Off duty', initials: application.gameName.split(' ').map(part => part[0]).join('').slice(0, 2).toUpperCase() || 'NE', invoices: 0, discord: application.discord, gameId: application.gameId, mobile: application.mobile, cid: application.cid };
    const nextStaff = [...staff, employee]; const nextApplications = applications.filter(item => item.id !== application.id);
    setStaff(nextStaff); setApplications(nextApplications);
    window.localStorage.setItem('mp-employees', JSON.stringify(nextStaff)); window.localStorage.setItem('mp-applications', JSON.stringify(nextApplications));
  };

  const rejectApplication = (id: string) => { const next = applications.filter(item => item.id !== id); setApplications(next); window.localStorage.setItem('mp-applications', JSON.stringify(next)); };

  const navigation: { label: View; icon: typeof LayoutDashboard; admin?: boolean }[] = [
    { label: 'Dashboard', icon: LayoutDashboard }, { label: 'Time Clock', icon: Clock3 }, { label: 'New Invoice', icon: ReceiptText },
    { label: 'Invoice History', icon: History }, { label: 'Employees', icon: Users }, { label: 'Join Team', icon: UserPlus }, { label: 'Reports', icon: BarChart3 },
    { label: 'Admin Controls', icon: ShieldCheck, admin: true },
  ];

  const renderHeader = (eyebrow: string, title: string, description: string) => <div className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><p className="text-[11px] font-black uppercase tracking-[.22em] text-[#52e0c4]">{eyebrow}</p><h1 className="mt-2 text-3xl font-black tracking-[-.035em] text-white sm:text-4xl">{title}</h1><p className="mt-2 max-w-2xl text-sm text-[#7897a4]">{description}</p></div><div className="flex items-center gap-2 rounded-full border border-[#1e3d48] bg-[#0b1820] px-3 py-2 text-xs text-[#88a3ad]"><span className="h-2 w-2 animate-pulse rounded-full bg-[#52e0c4]" /> Los Santos network online</div></div>;

  const DutyCard = () => <Panel className="p-5 sm:p-6"><div className="flex items-start justify-between"><div><div className="flex items-center gap-2 text-sm font-black uppercase tracking-[.08em]"><Clock3 className="text-[#52e0c4]" size={18} /> Duty status</div><p className="mt-2 text-xs text-[#7897a4]">Current shift tracked in real time.</p></div><span className={`rounded-full px-2.5 py-1 text-[10px] font-black uppercase ${checkedIn ? 'bg-[#52e0c4]/10 text-[#52e0c4]' : 'bg-white/5 text-[#7897a4]'}`}>{checkedIn ? 'On duty' : 'Off duty'}</span></div><div className="mt-8 flex flex-wrap items-end justify-between gap-5"><div><p className="text-[10px] font-black uppercase tracking-[.18em] text-[#56727e]">This session</p><p className="mt-1 font-mono text-4xl font-black text-white sm:text-5xl">{duration}</p></div><button onClick={() => setCheckedIn(value => !value)} className={`flex items-center gap-2 rounded-xl px-5 py-3 text-sm font-black uppercase tracking-[.08em] transition ${checkedIn ? 'bg-[#ef6b73] text-[#17080b] hover:bg-[#ff858c]' : 'bg-[#52e0c4] text-[#06221d] hover:bg-[#77ebd4]'}`}>{checkedIn ? <LogOut size={17} /> : <LogIn size={17} />}{checkedIn ? 'Check out' : 'Check in'}</button></div></Panel>;

  const EmployeeTable = () => <Panel><div className="flex flex-wrap items-center justify-between gap-3 border-b border-[#1e3d48] p-5"><div><p className="font-black text-white">Employee profiles</p><p className="mt-1 text-xs text-[#7897a4]">Weekly, monthly and invoice performance.</p></div>{isAdmin && <button onClick={() => setView('Admin Controls')} className="rounded-xl border border-[#2b5966] px-3 py-2 text-xs font-black text-[#b7d1da] hover:bg-[#102630]">Manage employees</button>}</div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left"><thead className="text-[10px] uppercase tracking-[.14em] text-[#56727e]"><tr><th className="px-5 py-3">Employee</th><th className="px-4 py-3">Weekly</th><th className="px-4 py-3">Monthly</th><th className="px-4 py-3">Invoices</th><th className="px-5 py-3 text-right">Status</th></tr></thead><tbody>{staff.map(employee => <tr key={employee.name} className="border-t border-[#142f39] hover:bg-[#0e2029]"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#15333d] text-xs font-black text-[#52e0c4]">{employee.initials}</span><div><p className="text-sm font-bold text-white">{employee.name}</p><p className="text-xs text-[#7897a4]">{employee.role}</p></div></div></td><td className="px-4 py-4 font-mono text-sm">{employee.week}</td><td className="px-4 py-4 font-mono text-sm text-[#a8c0c8]">{employee.month}</td><td className="px-4 py-4 text-sm">{employee.invoices}</td><td className={`px-5 py-4 text-right text-xs ${employee.status === 'On duty' ? 'text-[#52e0c4]' : 'text-[#6c8791]'}`}>● {employee.status}</td></tr>)}</tbody></table></div></Panel>;

  const TeamDirectory = () => <><Panel className="p-5 sm:p-6"><div className="flex items-center gap-3"><GitBranch className="text-[#52e0c4]" /><div><p className="font-black text-white">Mechanic hierarchy</p><p className="mt-1 text-xs text-[#7897a4]">Authority flows from Boss through the employee ranks.</p></div></div><div className="mt-6 overflow-x-auto"><div className="flex min-w-max items-center gap-2">{[...mechanicTypes].sort((a, b) => a.level - b.level).map((type, index) => <div key={type.id} className="flex items-center gap-2"><div className={`rounded-xl border px-4 py-3 text-center ${index === 0 ? 'border-[#52e0c4] bg-[#173a40]' : 'border-[#244b57] bg-[#0d222b]'}`}><p className="text-[9px] font-black uppercase tracking-[.14em] text-[#56727e]">Level {type.level}</p><p className={`mt-1 text-xs font-black uppercase ${index === 0 ? 'text-[#52e0c4]' : 'text-white'}`}>{type.name}</p><p className="mt-1 text-[10px] text-[#7897a4]">{staff.filter(employee => employee.role === type.name).length} staff</p></div>{index < mechanicTypes.length - 1 && <span className="text-[#31535e]">→</span>}</div>)}</div></div></Panel><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{[...staff].sort((a, b) => (mechanicTypes.find(type => type.name === a.role)?.level || 99) - (mechanicTypes.find(type => type.name === b.role)?.level || 99)).map(employee => <Panel key={`${employee.name}-${employee.cid || employee.initials}`} className="overflow-hidden"><div className="relative grid h-40 place-items-center bg-[radial-gradient(circle_at_top_right,#19444a,#0b1e27_55%,#071219)]"><span className="text-5xl font-black tracking-[-.08em] text-[#52e0c4]/70">{employee.initials}</span><span className={`absolute left-4 top-4 rounded-lg px-2 py-1 text-[9px] font-black uppercase ${employee.status === 'On duty' ? 'bg-[#52e0c4] text-[#06221d]' : 'bg-[#172f3a] text-[#9ab1b9]'}`}>{employee.status}</span><span className="absolute bottom-0 right-0 bg-[#52e0c4] px-3 py-1.5 text-[10px] font-black uppercase text-[#06221d]">{employee.role}</span></div><div className="p-5"><p className="text-lg font-black uppercase text-white">{employee.name}</p><div className="mt-3 grid grid-cols-2 gap-2 text-xs text-[#7897a4]"><span>Weekly <b className="block text-white">{employee.week}</b></span><span>Invoices <b className="block text-white">{employee.invoices}</b></span></div>{employee.mobile && <p className="mt-4 border-t border-[#17323c] pt-3 font-mono text-xs text-[#52e0c4]">☎ {employee.mobile}</p>}</div></Panel>)}</div></>;

  return <main className="min-h-screen bg-[#061016] text-[#d8e5e9]">
    <header className="sticky top-0 z-40 border-b border-[#18333d] bg-[#061016]/95 backdrop-blur-xl"><div className="mx-auto flex h-[74px] max-w-[1580px] items-center justify-between px-4 sm:px-7"><button onClick={() => setView('Dashboard')} className="flex items-center gap-3 text-left"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#52e0c4] text-sm font-black text-[#06221d] shadow-[0_0_30px_rgba(82,224,196,.2)]">MP</span><span><strong className="block max-w-[190px] truncate text-base font-black uppercase tracking-[.04em] text-white">{businessName}</strong><span className="text-[10px] font-black uppercase tracking-[.22em] text-[#52e0c4]">Operations</span></span></button><nav className="hidden items-center gap-6 lg:flex">{(['Dashboard', 'New Invoice', 'Employees', 'Join Team'] as View[]).map(label => <button key={label} onClick={() => setView(label)} className={`text-xs font-black uppercase tracking-[.12em] transition ${view === label ? 'text-[#52e0c4]' : 'text-[#688792] hover:text-white'}`}>{label}</button>)}</nav><div className="relative flex items-center gap-2"><button onClick={() => { if (isAdmin && applications.length) setView('Admin Controls'); }} className="relative rounded-xl border border-[#1e3d48] p-2.5 text-[#7897a4] hover:text-white" aria-label="Notifications"><Bell size={17} />{isAdmin && applications.length > 0 && <span className="absolute -right-1.5 -top-1.5 grid h-5 min-w-5 place-items-center rounded-full bg-[#ef6b73] px-1 text-[9px] font-black text-white">{applications.length}</span>}</button><button onClick={() => setMenuOpen(value => !value)} className="flex items-center gap-2 rounded-xl border border-[#1e3d48] bg-[#0b1820] p-1.5 pr-2.5"><span className="grid h-8 w-8 place-items-center rounded-lg bg-[#173540] text-[10px] font-black text-[#52e0c4]">{currentUser.initials}</span><span className="hidden text-left sm:block"><span className="block max-w-32 truncate text-xs font-bold text-white">{currentUser.name}</span><span className="block text-[9px] font-black uppercase tracking-[.1em] text-[#52e0c4]">{employeePreview ? currentUser.role : 'Administrator'}</span></span><ChevronDown size={14} /></button><button onClick={() => setMenuOpen(value => !value)} className="rounded-xl border border-[#1e3d48] p-2.5 lg:hidden" aria-label="Open navigation"><Menu size={17} /></button>{menuOpen && <div className="absolute right-0 top-14 w-64 rounded-2xl border border-[#244b57] bg-[#0b1820] p-2 shadow-2xl"><div className="border-b border-[#1e3d48] px-3 py-3"><p className="text-sm font-bold text-white">{currentUser.name}</p><p className="mt-1 text-xs text-[#7897a4]">{currentUser.role} · {employeePreview ? 'Employee' : 'Admin'}</p></div><button onClick={() => { setEmployeePreview(value => !value); setMenuOpen(false); setView('Dashboard'); }} className="mt-2 flex w-full items-center gap-2 rounded-xl px-3 py-2.5 text-left text-xs hover:bg-[#102630]"><UserRound size={15} />{employeePreview ? 'Return to admin view' : 'Preview employee view'}</button><button onClick={() => { setView('Admin Controls'); setMenuOpen(false); }} className="flex w-full items-center gap-2 rounded-xl px-3 py-2.5 text-left text-xs hover:bg-[#102630]"><Settings2 size={15} />Settings</button></div>}</div></div></header>

    <div className="mx-auto grid max-w-[1580px] grid-cols-1 lg:grid-cols-[238px_minmax(0,1fr)]"><aside className="hidden min-h-[calc(100vh-74px)] border-r border-[#18333d] px-4 py-6 lg:block"><p className="mb-3 px-3 text-[10px] font-black uppercase tracking-[.2em] text-[#46636e]">Operations</p>{navigation.filter(item => !item.admin).map(({ label, icon: Icon }) => <button key={label} onClick={() => setView(label)} className={`mb-1 flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm transition ${view === label ? 'bg-[#102a33] font-bold text-[#52e0c4]' : 'text-[#7897a4] hover:bg-[#0c1e26] hover:text-white'}`}><Icon size={17} />{label}</button>)}{isAdmin && <><p className="mb-3 mt-7 px-3 text-[10px] font-black uppercase tracking-[.2em] text-[#46636e]">Management</p>{navigation.filter(item => item.admin).map(({ label, icon: Icon }) => <button key={label} onClick={() => setView(label)} className={`mb-1 flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm transition ${view === label ? 'bg-[#102a33] font-bold text-[#52e0c4]' : 'text-[#7897a4] hover:bg-[#0c1e26] hover:text-white'}`}><Icon size={17} />{label}</button>)}</>}<div className="mt-8 rounded-2xl border border-[#24515d] bg-[#0d222b] p-4"><div className="flex items-center gap-2 text-xs font-black uppercase tracking-[.1em] text-[#52e0c4]"><Link2 size={15} /> Discord ready</div><p className="mt-2 text-xs leading-5 text-[#7897a4]">Bot hooks are reserved and can be connected later.</p>{isAdmin && <button onClick={() => setView('Admin Controls')} className="mt-3 text-xs font-bold text-white">Configure →</button>}</div></aside>

      <section className="min-w-0 p-4 sm:p-7 lg:p-8">
        {view === 'Dashboard' && <>{renderHeader('Operations console', `Welcome back, ${currentUser.name.split(' ')[0]}.`, 'Track your shift, monitor the garage and jump straight into today’s work.')}<div className="grid gap-5 xl:grid-cols-[1.35fr_.9fr]"><DutyCard /><Panel className="p-5 sm:p-6"><div className="flex items-center justify-between"><div className="font-black text-white">Hours overview</div><span className="rounded-lg bg-[#102a33] px-2 py-1 text-[10px] font-black text-[#52e0c4]">THIS WEEK</span></div><div className="mt-6 flex items-end gap-2"><span className="text-4xl font-black text-white">34.3</span><span className="mb-1 text-sm text-[#7897a4]">hours</span></div><div className="mt-5 flex h-14 items-end gap-2">{[34, 58, 42, 72, 51, 94, 28].map((height, index) => <span key={index} className="flex-1 rounded-t-md bg-gradient-to-t from-[#1b665f] to-[#52e0c4]" style={{ height: `${height}%`, opacity: index === 5 ? 1 : .55 }} />)}</div></Panel></div><div className="mt-5 grid gap-5 md:grid-cols-3"><button onClick={() => setView('New Invoice')} className="group rounded-2xl border border-[#1e3d48] bg-[#0b1820] p-5 text-left hover:border-[#52e0c4]/50"><ReceiptText className="text-[#52e0c4]" /><p className="mt-5 font-black text-white">Create invoice</p><p className="mt-1 text-xs text-[#7897a4]">Build, save and copy a customer bill.</p></button><button onClick={() => setView('Employees')} className="rounded-2xl border border-[#1e3d48] bg-[#0b1820] p-5 text-left hover:border-[#52e0c4]/50"><Users className="text-[#52e0c4]" /><p className="mt-5 font-black text-white">Employee profiles</p><p className="mt-1 text-xs text-[#7897a4]">Review ranks and working hours.</p></button><button onClick={() => setView(isAdmin && applications.length ? 'Admin Controls' : 'Join Team')} className="rounded-2xl border border-[#1e3d48] bg-[#0b1820] p-5 text-left hover:border-[#52e0c4]/50">{isAdmin ? <Inbox className="text-[#52e0c4]" /> : <UserPlus className="text-[#52e0c4]" />}<p className="mt-5 font-black text-white">{isAdmin && applications.length ? `${applications.length} pending application${applications.length === 1 ? '' : 's'}` : 'Join the team'}</p><p className="mt-1 text-xs text-[#7897a4]">{isAdmin && applications.length ? 'Review new employee requests.' : 'Submit an employee application.'}</p></button></div><div className="mt-5"><EmployeeTable /></div></>}

        {view === 'Time Clock' && <>{renderHeader('Duty management', 'Time clock', 'Check in and out, then review your working-hour totals.')}<div className="grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><DutyCard /><Panel className="p-6"><p className="text-sm font-black uppercase tracking-[.08em] text-white">Your totals</p><div className="mt-6 grid grid-cols-2 gap-3"><div className="rounded-xl bg-[#0f252e] p-4"><p className="text-xs text-[#7897a4]">This week</p><p className="mt-2 text-2xl font-black text-white">34h 20m</p></div><div className="rounded-xl bg-[#0f252e] p-4"><p className="text-xs text-[#7897a4]">This month</p><p className="mt-2 text-2xl font-black text-white">138h 10m</p></div></div><p className="mt-5 text-xs leading-5 text-[#7897a4]">The session timer pauses when you check out. Admins can review all employee shifts from Employee Profiles.</p></Panel></div></>}

        {view === 'New Invoice' && <>{renderHeader('Billing workspace', 'Create an invoice', 'Choose parts, add custom labor and submit. The completed invoice is saved and copied to your clipboard automatically.')}<div className="grid gap-5 2xl:grid-cols-[minmax(0,1fr)_390px]"><Panel className="p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="font-black text-white">Parts & services</p><p className="mt-1 text-xs text-[#7897a4]">Use the quantity controls to build the bill.</p></div><Wrench className="text-[#52e0c4]" /></div><div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{catalog.map(service => <div key={service.id} className={`rounded-2xl border p-4 transition ${items[service.id] ? 'border-[#52e0c4]/50 bg-[#102a33]' : 'border-[#1e3d48] bg-[#08141a]'}`}><div className="flex items-start justify-between"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#15333d] text-[10px] font-black tracking-[.08em] text-[#52e0c4]">{service.icon}</span><span className="font-mono text-sm font-bold text-white">{money(service.price)}</span></div><p className="mt-4 text-sm font-black text-white">{service.name}</p><p className="mt-1 text-[11px] text-[#64818c]">{service.unit}</p><div className="mt-5 grid grid-cols-[40px_1fr_40px] items-center gap-2"><button onClick={() => changeQty(service.id, -1)} className="grid h-9 place-items-center rounded-xl bg-[#311b24] text-[#ef8490] hover:bg-[#49202b]" aria-label={`Remove ${service.name}`}><Minus size={15} /></button><span className="text-center font-mono text-lg font-black text-white">{items[service.id] || 0}</span><button onClick={() => changeQty(service.id, 1)} className="grid h-9 place-items-center rounded-xl bg-[#17443f] text-[#72ead5] hover:bg-[#1d5a51]" aria-label={`Add ${service.name}`}><Plus size={15} /></button></div></div>)}</div><div className="mt-5 grid gap-3 border-t border-[#1e3d48] pt-5 md:grid-cols-2"><label className="text-xs font-bold text-[#7897a4]">Customer / character name <span className="font-normal text-[#52e0c4]">(optional)</span><input value={customer} onChange={event => setCustomer(event.target.value)} placeholder="Leave blank for walk-in customer" className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label><label className="text-xs font-bold text-[#7897a4]">Discount percentage<input type="number" min="0" max="100" value={discount} onChange={event => setDiscount(Math.min(100, Math.max(0, Number(event.target.value))))} className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label><label className="text-xs font-bold text-[#7897a4]">Custom labor description<input value={laborName} onChange={event => setLaborName(event.target.value)} placeholder="e.g. Engine calibration" className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label><label className="text-xs font-bold text-[#7897a4]">Custom labor charge<input type="number" min="0" value={laborPrice || ''} onChange={event => setLaborPrice(Math.max(0, Number(event.target.value)))} placeholder="$0" className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label></div></Panel><Panel className="h-fit overflow-hidden"><div className="border-b border-[#1e3d48] p-5"><div className="flex items-center justify-between"><div><p className="font-black text-white">Live invoice</p><p className="mt-1 text-xs text-[#7897a4]">Next number · MP-{1847 + invoices.length}</p></div><FileText className="text-[#52e0c4]" size={20} /></div></div><div className="min-h-52 p-5">{subtotal <= 0 ? <div className="grid min-h-40 place-items-center text-center"><div><ReceiptText className="mx-auto text-[#31535e]" /><p className="mt-3 text-sm text-[#64818c]">Your selected items will appear here.</p></div></div> : <div className="space-y-3">{catalog.filter(service => items[service.id]).map(service => <div key={service.id} className="flex justify-between gap-3 text-sm"><span className="text-[#92abb4]">{items[service.id]}× {service.name}</span><span className="font-mono text-white">{money(service.price * items[service.id])}</span></div>)}{laborPrice > 0 && <div className="flex justify-between gap-3 text-sm"><span className="text-[#92abb4]">Labor · {laborName || 'Custom labor'}</span><span className="font-mono text-white">{money(laborPrice)}</span></div>}</div>}</div><div className="border-t border-dashed border-[#2b4c57] p-5"><div className="flex justify-between text-sm text-[#7897a4]"><span>Subtotal</span><span>{money(subtotal)}</span></div>{discount > 0 && <div className="mt-2 flex justify-between text-sm text-[#52e0c4]"><span>Discount ({discount}%)</span><span>−{money(subtotal - total)}</span></div>}<div className="mt-5 flex items-end justify-between"><span className="font-black text-white">Total</span><span className="font-mono text-4xl font-black text-[#52e0c4]">{money(total)}</span></div><div className="mt-5 rounded-xl bg-[#071219] p-3 text-xs leading-5 text-[#7897a4]"><span className="font-bold text-[#a9c0c8]">Admin message:</span> {adminMessage}</div></div><button onClick={submitInvoice} className="flex w-full items-center justify-center gap-2 bg-[#52e0c4] px-5 py-4 text-sm font-black uppercase tracking-[.12em] text-[#06221d] hover:bg-[#77ebd4]"><Clipboard size={17} /> Submit & copy invoice</button><button onClick={resetInvoice} className="w-full py-3 text-xs font-black uppercase tracking-[.12em] text-[#64818c] hover:text-white">Reset invoice</button>{copyStatus && <p className={`border-t border-[#1e3d48] px-5 py-4 text-center text-xs font-bold ${copyStatus.includes('copied') ? 'text-[#52e0c4]' : 'text-[#ef8490]'}`}>{copyStatus}</p>}</Panel></div></>}

        {view === 'Invoice History' && <>{renderHeader('Billing archive', 'Invoice history', 'Every submitted invoice is stored here and can be copied again in one click.')}<Panel><div className="divide-y divide-[#17323c]">{invoices.map(invoice => <div key={invoice.id} className="grid gap-4 p-5 md:grid-cols-[110px_1fr_130px_auto] md:items-center"><div><p className="font-mono text-sm font-black text-[#52e0c4]">{invoice.id}</p><p className="mt-1 text-[10px] text-[#64818c]">{invoice.date}</p></div><div><p className="font-bold text-white">{invoice.customer}</p><p className="mt-1 truncate text-xs text-[#7897a4]">{invoice.lines.join(' · ')}</p></div><p className="font-mono text-lg font-black text-white">{money(invoice.total)}</p><button onClick={async () => { await copyText(makeInvoiceText(invoice)); setCopyStatus(`${invoice.id} copied to clipboard.`); }} className="flex items-center justify-center gap-2 rounded-xl border border-[#2a5663] px-3 py-2 text-xs font-black text-[#b7d1da] hover:bg-[#102a33]"><Clipboard size={14} /> Copy</button></div>)}</div></Panel>{copyStatus && <p className="mt-4 text-sm font-bold text-[#52e0c4]">{copyStatus}</p>}</>}

        {view === 'Employees' && <>{renderHeader('Our team', 'Meet the crew', 'The employee directory follows the administrator-defined mechanic hierarchy, beginning with the Boss.')}<TeamDirectory /></>}

        {view === 'Join Team' && <>{renderHeader('Employee registration', 'Apply to join the garage', 'Complete all five in-game identity fields. Your account is created only after an admin approves it and assigns your mechanic type.')}<Panel className="mx-auto max-w-3xl p-5 sm:p-7"><div className="flex items-center gap-3 border-b border-[#1e3d48] pb-5"><span className="grid h-11 w-11 place-items-center rounded-xl bg-[#15333d] text-[#52e0c4]"><UserPlus size={20} /></span><div><p className="font-black text-white">New employee application</p><p className="mt-1 text-xs text-[#7897a4]">Every field is required for staff verification.</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold text-[#7897a4]">Discord Tag<input value={applicationForm.discord} onChange={event => setApplicationForm(current => ({ ...current, discord: event.target.value }))} placeholder="username or username#0000" className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label><label className="text-xs font-bold text-[#7897a4]">In Game ID<input value={applicationForm.gameId} onChange={event => setApplicationForm(current => ({ ...current, gameId: event.target.value }))} placeholder="Server ID" className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label><label className="text-xs font-bold text-[#7897a4]">In Game Name<input value={applicationForm.gameName} onChange={event => setApplicationForm(current => ({ ...current, gameName: event.target.value }))} placeholder="Character name" className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label><label className="text-xs font-bold text-[#7897a4]">In Game Mobile Number<input value={applicationForm.mobile} onChange={event => setApplicationForm(current => ({ ...current, mobile: event.target.value }))} placeholder="555-0000" className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label><label className="text-xs font-bold text-[#7897a4] sm:col-span-2">In Game CID<input value={applicationForm.cid} onChange={event => setApplicationForm(current => ({ ...current, cid: event.target.value }))} placeholder="Character CID" className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label></div><button onClick={submitApplication} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-[#52e0c4] px-5 py-4 text-sm font-black uppercase tracking-[.1em] text-[#06221d]"><UserPlus size={17} /> Submit for admin approval</button>{applicationStatus && <p className={`mt-4 rounded-xl p-3 text-center text-xs font-bold ${applicationStatus.includes('submitted') ? 'bg-[#12342f] text-[#52e0c4]' : 'bg-[#311b24] text-[#ef8490]'}`}>{applicationStatus}</p>}</Panel></>}

        {view === 'Reports' && <>{renderHeader('Performance data', 'Garage reports', 'A quick look at hours, revenue and completed work.')}<div className="grid gap-5 md:grid-cols-3"><Panel className="p-6"><Gauge className="text-[#52e0c4]" /><p className="mt-6 text-3xl font-black text-white">102.6h</p><p className="mt-1 text-xs text-[#7897a4]">Team hours this week</p></Panel><Panel className="p-6"><ReceiptText className="text-[#52e0c4]" /><p className="mt-6 text-3xl font-black text-white">{invoices.length + 244}</p><p className="mt-1 text-xs text-[#7897a4]">Invoices completed</p></Panel><Panel className="p-6"><BarChart3 className="text-[#52e0c4]" /><p className="mt-6 text-3xl font-black text-white">{money(invoices.reduce((sum, invoice) => sum + invoice.total, 0) + 46820)}</p><p className="mt-1 text-xs text-[#7897a4]">Recorded revenue</p></Panel></div></>}

        {view === 'Admin Controls' && isAdmin && <>
          {renderHeader('Full access management', 'Admin controls', 'Administrators can change business details, every catalog price, invoice text and employee profile information.')}
          <div className="space-y-5">
            <Panel className="overflow-hidden"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-[#1e3d48] p-5"><div className="flex items-center gap-3"><span className="relative grid h-10 w-10 place-items-center rounded-xl bg-[#15333d] text-[#52e0c4]"><Inbox size={19} />{applications.length > 0 && <span className="absolute -right-2 -top-2 grid h-5 min-w-5 place-items-center rounded-full bg-[#ef6b73] px-1 text-[9px] font-black text-white">{applications.length}</span>}</span><div><p className="font-black text-white">Employee applications</p><p className="mt-1 text-xs text-[#7897a4]">Approve an applicant and choose their mechanic type.</p></div></div><span className="text-xs font-bold text-[#52e0c4]">{applications.length} pending</span></div>{applications.length === 0 ? <div className="p-8 text-center"><Check className="mx-auto text-[#31535e]" /><p className="mt-3 text-sm text-[#7897a4]">No employee applications are waiting.</p></div> : <div className="divide-y divide-[#17323c]">{applications.map(application => <div key={application.id} className="grid gap-4 p-5 xl:grid-cols-[1fr_1fr_210px_auto] xl:items-center"><div><p className="font-black text-white">{application.gameName}</p><p className="mt-1 text-xs text-[#7897a4]">Discord: {application.discord}</p><p className="mt-1 text-xs text-[#7897a4]">Submitted {application.requestedAt}</p></div><div className="grid grid-cols-3 gap-2 text-xs"><span className="rounded-lg bg-[#0f252e] p-2 text-[#7897a4]">ID<b className="block text-white">{application.gameId}</b></span><span className="rounded-lg bg-[#0f252e] p-2 text-[#7897a4]">CID<b className="block text-white">{application.cid}</b></span><span className="rounded-lg bg-[#0f252e] p-2 text-[#7897a4]">Mobile<b className="block text-white">{application.mobile}</b></span></div><select value={application.assignedRole} onChange={event => setApplications(current => current.map(item => item.id === application.id ? { ...item, assignedRole: event.target.value } : item))} className="rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none">{[...mechanicTypes].sort((a, b) => a.level - b.level).map(type => <option key={type.id}>{type.name}</option>)}</select><div className="flex gap-2"><button onClick={() => acceptApplication(application)} className="rounded-xl bg-[#52e0c4] px-4 py-3 text-xs font-black text-[#06221d]">Accept</button><button onClick={() => rejectApplication(application.id)} className="grid h-10 w-10 place-items-center rounded-xl bg-[#311b24] text-[#ef8490]" aria-label={`Reject ${application.gameName}`}><X size={16} /></button></div></div>)}</div>}</Panel>
            <Panel className="p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><GitBranch className="text-[#52e0c4]" /><div><p className="font-black text-white">Mechanic type hierarchy</p><p className="mt-1 text-xs text-[#7897a4]">Boss remains first; add, rename, remove or reorder every other type.</p></div></div><button onClick={() => setMechanicTypes(current => [...current, { id: `type-${Date.now()}`, name: 'New Mechanic Type', level: Math.max(0, ...current.map(type => type.level)) + 1 }])} className="flex items-center gap-2 rounded-xl border border-[#2a5663] px-3 py-2 text-xs font-black text-[#b7d1da]"><Plus size={14} /> Add type</button></div><div className="mt-5 space-y-2">{[...mechanicTypes].sort((a, b) => a.level - b.level).map((type, index) => <div key={type.id} className="grid grid-cols-[56px_1fr_90px_auto] items-center gap-2 rounded-xl border border-[#17323c] bg-[#08141a] p-3"><span className={`grid h-9 w-9 place-items-center rounded-lg text-xs font-black ${index === 0 ? 'bg-[#52e0c4] text-[#06221d]' : 'bg-[#15333d] text-[#52e0c4]'}`}>{type.level}</span><input value={type.name} disabled={index === 0} onChange={event => { const oldName = type.name; const newName = event.target.value; setMechanicTypes(current => current.map(item => item.id === type.id ? { ...item, name: newName } : item)); setStaff(current => current.map(employee => employee.role === oldName ? { ...employee, role: newName } : employee)); }} className="rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm text-white outline-none disabled:opacity-70" /><input type="number" min={index === 0 ? 1 : 2} value={type.level} disabled={index === 0} onChange={event => setMechanicTypes(current => current.map(item => item.id === type.id ? { ...item, level: Math.max(index === 0 ? 1 : 2, Number(event.target.value)) } : item))} className="rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm text-white outline-none disabled:opacity-70" />{index === 0 ? <span className="text-center text-[10px] font-black uppercase text-[#52e0c4]">Root</span> : <button onClick={() => { setMechanicTypes(current => current.filter(item => item.id !== type.id)); setStaff(current => current.map(employee => employee.role === type.name ? { ...employee, role: 'Mechanic' } : employee)); }} className="rounded-lg bg-[#311b24] px-3 py-2 text-xs font-black text-[#ef8490]">Remove</button>}</div>)}</div></Panel>
            <div className="grid gap-5 xl:grid-cols-[.75fr_1.25fr]">
              <Panel className="p-6"><div className="flex items-center gap-3"><Settings2 className="text-[#52e0c4]" /><div><p className="font-black text-white">Business settings</p><p className="mt-1 text-xs text-[#7897a4]">Used in the navigation and copied invoices.</p></div></div><label className="mt-5 block text-xs font-bold text-[#7897a4]">Business name<input value={businessName} onChange={event => setBusinessName(event.target.value)} className="mt-2 w-full rounded-xl border border-[#244b57] bg-[#071219] px-3 py-3 text-sm text-white outline-none focus:border-[#52e0c4]" /></label><label className="mt-4 block text-xs font-bold text-[#7897a4]">Automatic invoice message<textarea value={adminMessage} onChange={event => setAdminMessage(event.target.value)} rows={5} className="mt-2 w-full resize-none rounded-xl border border-[#244b57] bg-[#071219] p-4 text-sm leading-6 text-white outline-none focus:border-[#52e0c4]" /></label></Panel>
              <Panel className="p-6"><div className="flex items-center gap-3"><Wrench className="text-[#52e0c4]" /><div><p className="font-black text-white">Service catalog & prices</p><p className="mt-1 text-xs text-[#7897a4]">Changes update the invoice calculator immediately.</p></div></div><div className="mt-5 space-y-3">{catalog.map(service => <div key={service.id} className="grid gap-2 rounded-xl border border-[#17323c] bg-[#08141a] p-3 sm:grid-cols-[1fr_1fr_120px]"><label className="text-[10px] font-black uppercase tracking-[.1em] text-[#56727e]">Service name<input value={service.name} onChange={event => setCatalog(current => current.map(item => item.id === service.id ? { ...item, name: event.target.value } : item))} className="mt-1 w-full rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm normal-case tracking-normal text-white outline-none" /></label><label className="text-[10px] font-black uppercase tracking-[.1em] text-[#56727e]">Description<input value={service.unit} onChange={event => setCatalog(current => current.map(item => item.id === service.id ? { ...item, unit: event.target.value } : item))} className="mt-1 w-full rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm normal-case tracking-normal text-white outline-none" /></label><label className="text-[10px] font-black uppercase tracking-[.1em] text-[#56727e]">Price<input type="number" min="0" value={service.price} onChange={event => setCatalog(current => current.map(item => item.id === service.id ? { ...item, price: Math.max(0, Number(event.target.value)) } : item))} className="mt-1 w-full rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 font-mono text-sm normal-case tracking-normal text-white outline-none" /></label></div>)}</div></Panel>
            </div>
            <Panel className="p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><Users className="text-[#52e0c4]" /><div><p className="font-black text-white">Employee management</p><p className="mt-1 text-xs text-[#7897a4]">Edit names, roles, hours, invoice totals and duty status.</p></div></div><button onClick={() => setStaff(current => [...current, { name: 'New Employee', role: 'Mechanic', week: '0h 00m', month: '0h 00m', status: 'Off duty', initials: 'NE', invoices: 0 }])} className="flex items-center gap-2 rounded-xl border border-[#2a5663] px-3 py-2 text-xs font-black text-[#b7d1da]"><Plus size={14} /> Add employee</button></div><div className="mt-5 space-y-3">{staff.map((employee, index) => <div key={`${employee.initials}-${index}`} className="grid gap-2 rounded-xl border border-[#17323c] bg-[#08141a] p-3 md:grid-cols-2 xl:grid-cols-[1.2fr_1fr_.75fr_.75fr_100px_110px_auto]"><input aria-label="Employee name" value={employee.name} onChange={event => setStaff(current => current.map((item, itemIndex) => itemIndex === index ? { ...item, name: event.target.value, initials: event.target.value.split(' ').map(part => part[0]).join('').slice(0, 2).toUpperCase() || 'NA' } : item))} className="rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm text-white outline-none" /><input aria-label="Employee role" value={employee.role} onChange={event => setStaff(current => current.map((item, itemIndex) => itemIndex === index ? { ...item, role: event.target.value } : item))} className="rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm text-white outline-none" /><input aria-label="Weekly hours" value={employee.week} onChange={event => setStaff(current => current.map((item, itemIndex) => itemIndex === index ? { ...item, week: event.target.value } : item))} className="rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm text-white outline-none" /><input aria-label="Monthly hours" value={employee.month} onChange={event => setStaff(current => current.map((item, itemIndex) => itemIndex === index ? { ...item, month: event.target.value } : item))} className="rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm text-white outline-none" /><input aria-label="Invoice count" type="number" min="0" value={employee.invoices} onChange={event => setStaff(current => current.map((item, itemIndex) => itemIndex === index ? { ...item, invoices: Math.max(0, Number(event.target.value)) } : item))} className="rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm text-white outline-none" /><select aria-label="Duty status" value={employee.status} onChange={event => setStaff(current => current.map((item, itemIndex) => itemIndex === index ? { ...item, status: event.target.value } : item))} className="rounded-lg border border-[#244b57] bg-[#061016] px-3 py-2 text-sm text-white outline-none"><option>On duty</option><option>Off duty</option><option>On leave</option></select><button onClick={() => setStaff(current => current.filter((_, itemIndex) => itemIndex !== index))} className="rounded-lg bg-[#311b24] px-3 py-2 text-xs font-black text-[#ef8490]">Remove</button></div>)}</div></Panel>
            <div className="grid gap-5 xl:grid-cols-[1fr_.65fr]"><Panel className="p-6"><div className="flex items-center gap-3"><ShieldCheck className="text-[#52e0c4]" /><div><p className="font-black text-white">Administrator permissions</p><p className="mt-1 text-xs text-[#7897a4]">Admin accounts have full control. Employees cannot see this area.</p></div></div><div className="mt-5 grid gap-2 sm:grid-cols-2">{['Change every service price', 'Edit business and invoice text', 'Add, edit or remove employees', 'Review every shift and invoice', 'Change roles and duty status', 'Configure the Discord bot'].map(permission => <div key={permission} className="flex items-center gap-2 rounded-xl bg-[#0f252e] p-3 text-xs text-[#a8c0c8]"><Check size={14} className="text-[#52e0c4]" />{permission}</div>)}</div></Panel><Panel className="p-6"><div className="flex items-center gap-3"><Link2 className="text-[#52e0c4]" /><div><p className="font-black text-white">Discord integration</p><p className="mt-1 text-xs text-[#7897a4]">Bot credentials and channel mapping can be added later.</p></div></div></Panel></div>
            <div className="sticky bottom-4 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-[#2a5663] bg-[#0d222b]/95 p-4 shadow-2xl backdrop-blur"><p className="text-xs text-[#91aab3]">Save to apply these settings across this device.</p><button onClick={saveAdminSettings} className="flex items-center gap-2 rounded-xl bg-[#52e0c4] px-5 py-3 text-sm font-black text-[#06221d]"><Check size={16} /> Save all changes</button>{copyStatus && <p className="w-full text-right text-xs font-bold text-[#52e0c4]">{copyStatus}</p>}</div>
          </div>
        </>}

        {view === 'Admin Controls' && !isAdmin && <Panel className="mx-auto max-w-lg p-8 text-center"><ShieldCheck className="mx-auto text-[#ef8490]" /><h1 className="mt-4 text-xl font-black text-white">Administrator access required</h1><p className="mt-2 text-sm text-[#7897a4]">Management controls are hidden from employee accounts.</p></Panel>}
      </section>
    </div>
  </main>;
}
